# Drop Existing Theme Here

Place the existing theme folder/files in this directory.

Then instruct the coding agent:

> Review `docs/THEME-INTEGRATION.md`, inventory `theme-source/`, and integrate the theme into Laravel Blade layouts/components without changing domain logic, routes, permissions, finance rules, or safety rules.

Do not delete this README until theme integration is complete.
