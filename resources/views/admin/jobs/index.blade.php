<x-layouts.admin title="Jobs"><div class="grid gap-4">@forelse($jobs as $job)<article class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-black">#{{ $job->id }} · {{ $job->serviceRequest->service->name }}</p><p class="text-sm text-slate-600">{{ $job->serviceFinder->name }} → {{ $job->provider->name }}</p></div><span class="font-bold">{{ str($job->status->value)->replace('_', ' ')->title() }}</span></div><p class="mt-3">PHP {{ $job->agreed_price }}</p></article>@empty<p>No jobs.</p>@endforelse {{ $jobs->links() }}</div></x-layouts.admin>
