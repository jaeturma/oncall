<x-layouts.admin title="Enforcement cases"><div class="grid gap-4">@forelse($cases as $case)<a class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200" href="{{ route('admin.enforcement.show', $case) }}"><div class="flex justify-between gap-3"><p class="font-black">{{ $case->user->name }} · {{ str($case->violation_category->value)->replace('_', ' ')->title() }}</p><span class="font-bold">{{ str($case->status->value)->replace('_', ' ')->title() }}</span></div><p class="mt-2 text-sm text-slate-600">Reported by {{ $case->relatedReport?->reporter?->name ?? 'System flag' }} · {{ str($case->severity->value)->title() }}</p></a>@empty<p>No enforcement cases.</p>@endforelse {{ $cases->links() }}</div></x-layouts.admin>
