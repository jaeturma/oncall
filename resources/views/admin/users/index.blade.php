<x-layouts.admin title="Users"><form class="mb-5 flex flex-wrap gap-3 rounded-xl bg-white p-4 ring-1 ring-slate-200"><input class="rounded-lg border-slate-300" name="q" value="{{ request('q') }}" placeholder="Name or email"><select class="rounded-lg border-slate-300" name="role"><option value="">All roles</option>@foreach(App\Enums\UserRole::cases() as $role)<option value="{{ $role->value }}" @selected(request('role') === $role->value)>{{ str($role->value)->replace('_', ' ')->title() }}</option>@endforeach</select><select class="rounded-lg border-slate-300" name="status"><option value="">All statuses</option>@foreach(App\Enums\UserStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ str($status->value)->replace('_', ' ')->title() }}</option>@endforeach</select><button class="rounded-lg bg-navy-900 px-4 py-2 font-bold text-white">Filter</button></form><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">User</th><th class="p-4">Role</th><th class="p-4">Status</th><th class="p-4">Identity</th></tr></thead><tbody>@foreach($users as $user)<tr class="border-t border-slate-200"><td class="p-4"><p class="font-bold">{{ $user->name }}</p><p class="text-slate-500">{{ $user->email }}</p></td><td class="p-4">{{ str($user->role->value)->replace('_', ' ')->title() }}</td><td class="p-4">{{ str($user->status->value)->replace('_', ' ')->title() }}</td><td class="p-4">{{ str($user->identity_verification_status->value)->title() }}</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $users->links() }}</div></x-layouts.admin>
