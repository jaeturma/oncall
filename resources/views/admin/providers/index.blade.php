<x-layouts.admin title="Providers"><div class="grid gap-4">@forelse($providers as $profile)<article class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-black">{{ $profile->user->name }}</h2><p class="text-sm text-slate-600">{{ $profile->user->email }} · {{ $profile->municipality->name }}, {{ $profile->province->name }}</p></div><span class="font-bold">{{ str($profile->verification_status->value)->title() }}</span></div><p class="mt-3 text-sm">{{ $profile->provider_services_count }} services · Rating {{ $profile->rating_cached ?? 'New' }} · {{ $profile->completed_jobs_cached }} completed</p></article>@empty<p>No providers.</p>@endforelse {{ $providers->links() }}</div></x-layouts.admin>
