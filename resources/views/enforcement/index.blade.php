<x-layouts.app title="Safety cases"><div class="grid gap-4">@forelse($cases as $case)<a class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200" href="{{ route('enforcement-cases.show', $case) }}"><div class="flex justify-between gap-3"><p class="font-black">{{ str($case->violation_category->value)->replace('_', ' ')->title() }}</p><span class="text-sm font-bold">{{ str($case->status->value)->replace('_', ' ')->title() }}</span></div><p class="mt-2 text-sm text-slate-600">Action: {{ $case->action ? str($case->action->value)->replace('_', ' ')->title() : 'Awaiting admin review' }}</p></a>@empty<p class="rounded-2xl bg-white p-8 text-slate-600 shadow-sm ring-1 ring-slate-200">No enforcement cases affect your account.</p>@endforelse {{ $cases->links() }}</div></x-layouts.app>
